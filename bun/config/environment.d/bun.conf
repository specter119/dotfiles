# Bun 1.4.2 still ignores [install].globalDir for -g commands.
# Keep these environment variables until oven-sh/bun#35454 is resolved.
BUN_INSTALL_GLOBAL_DIR=$HOME/.local/share/bun
BUN_INSTALL_BIN=$HOME/.local/bin
