{
  "github-copilot": {
    "access": "{{replace (command_output 'rbw get github-copilot-access') '\n' ''}}",
    "expires": 0,
    "refresh": "{{replace (command_output 'rbw get github-copilot-refresh') '\n' ''}}",
    "type": "oauth"
  },
  "packy-codex": {
    "key": "{{replace (command_output 'rbw get packy-codex-sub-key') '\n' ''}}",
    "type": "api"
  },
  "zai-coding-plan": {
    "key": "{{replace (command_output 'rbw get zai-coding-api-key') '\n' ''}}",
    "type": "api"
  }
}
