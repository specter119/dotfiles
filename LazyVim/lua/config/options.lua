-- Options are automatically loaded before lazy.nvim startup
-- Default options that are always set: https://github.com/LazyVim/LazyVim/blob/main/lua/lazyvim/config/options.lua
-- Add any additional options here

-- Enable project-local config (`.nvim.lua` / `.exrc`) for per-project overrides.
-- This does NOT change any default behavior: it only permits trusted project-local
-- config files to run in directories that contain one. Directories without a
-- `.nvim.lua` are completely unaffected.
vim.o.exrc = true

-- Enable project-local config (`.nvim.lua` / `.exrc`) for per-project overrides.
-- This does NOT change any default behavior: it only permits trusted project-local
-- config files to run in directories that contain one. Directories without a
-- `.nvim.lua` are completely unaffected.
vim.o.exrc = true
