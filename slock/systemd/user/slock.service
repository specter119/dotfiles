[Unit]
Description=Slock Daemon
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
WorkingDirectory=%h/.config/slock
ExecStartPre=bun update --latest --force @slock-ai/daemon
ExecStart=bun run slock-daemon --server-url https://api.slock.ai --api-key {{slock.api_key}}
{{#if slock.wss_proxy}}
Environment=WSS_PROXY={{slock.wss_proxy}}
{{/if}}
Restart=always
RestartSec=5

[Install]
WantedBy=default.target
