{
  "opencode": {
    "key": "{{replace (command_output 'rbw get opencode_apikey') '\n' ''}}",
    "type": "api_key"
  },
  "opencode-go": {
    "key": "{{replace (command_output 'rbw get opencode_apikey') '\n' ''}}",
    "type": "api_key"
  }
}
