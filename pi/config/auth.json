{
  "opencode-go": {
    "key": "{{replace (command_output 'rbw get opencode_go_apikey') '\n' ''}}",
    "type": "api_key"
  },
  "qoder": {
    "key": "{{replace (command_output 'podman secret inspect qoder-proxy-api-key --showsecret | jq -r .[0].SecretData') '\n' ''}}",
    "type": "api_key"
  }
}
